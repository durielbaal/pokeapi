/**
 * Input ports: These are the interfaces that define how other external components interact with the domain.
 * Examples: API or event controllers.
 * Output ports: These are the interfaces that the domain uses to interact with the external world
 * (databases, external APIs).
 * Examples: repositories.
 */
package com.myke.studios.infrastructure;